# Test vectors

AGPL-3.0-or-later

These vectors are used to ensure Node and Python implementations validate the same claims and reject the same invalid inputs.

Planned:
- payload normalization inputs (email lowercasing)
- TTL / exp validation (with skew)
- typ/aud/purpose enforcement
- store consume semantics

